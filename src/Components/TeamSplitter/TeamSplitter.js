import React from 'react';
import Button from "../../Components/Button/Button";


const TeamSplitter = ({ handleSubmit }) => (
   <Button handleClick = { handleSubmit }>Split</Button>
);

export default TeamSplitter;